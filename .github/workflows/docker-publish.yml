# cspell:ignore Buildx buildx DOCKERHUB
name: Docker Build and Push

on:
  push:
    tags:
      - "v*" # 当推送 v 开头的标签时触发，例如 v1.0.0

env:
  # 替换为你的 Docker Hub 用户名/组织名
  IMAGE_NAME: xcy960815/study-java

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Extract metadata (tags, labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMAGE_NAME }}
          tags: |
            type=semver,pattern={{version}}
            type=raw,value=latest

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          platforms: linux/amd64,linux/arm64 # 如果需要多架构构建
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # - name: Update Docker Hub repository overview from README
      #   env:
      #     DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
      #     DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
      #   run: |
      #     # 将 README.md 转成 JSON 安全的字符串
      #     DESCRIPTION=$(jq -Rs . < readme.md)
      #
      #     # 使用 Basic Auth (用户名:Personal Access Token)
      #     # 将用户名和令牌编码为 Base64 (兼容 Linux 和 macOS)
      #     AUTH_STRING=$(echo -n "${DOCKERHUB_USERNAME}:${DOCKERHUB_TOKEN}" | base64 | tr -d '\n')
      #
      #     # 先测试 token 是否有效（获取用户信息）
      #     echo "测试 Docker Hub token 有效性..."
      #     USER_INFO=$(curl -s --request GET \
      #       --url https://hub.docker.com/v2/users/${DOCKERHUB_USERNAME}/ \
      #       --header "Authorization: Basic ${AUTH_STRING}" || echo "FAILED")
      #
      #     if [ "$USER_INFO" = "FAILED" ]; then
      #       echo "错误: Token 验证失败，请检查 DOCKERHUB_USERNAME 和 DOCKERHUB_TOKEN"
      #       exit 1
      #     fi
      #
      #     echo "Token 验证成功，开始更新仓库描述..."
      #
      #     # 调用 Docker Hub API 更新仓库 full_description（Overview）
      #     RESPONSE=$(curl -s -w "\nHTTP_STATUS:%{http_code}" \
      #       --request PATCH \
      #       --url https://hub.docker.com/v2/repositories/xcy960815/study-java/ \
      #       --header "Content-Type: application/json" \
      #       --header "Authorization: Basic ${AUTH_STRING}" \
      #       --data "{\"full_description\": ${DESCRIPTION}}")
      #
      #     HTTP_STATUS=$(echo "$RESPONSE" | grep "HTTP_STATUS:" | cut -d: -f2)
      #     BODY=$(echo "$RESPONSE" | sed '/HTTP_STATUS:/d')
      #
      #     if [ "$HTTP_STATUS" != "200" ]; then
      #       echo "错误: API 调用失败，HTTP 状态码: $HTTP_STATUS"
      #       echo "响应内容: $BODY"
      #       exit 1
      #     fi
      #
      #     echo "仓库描述更新成功！"
